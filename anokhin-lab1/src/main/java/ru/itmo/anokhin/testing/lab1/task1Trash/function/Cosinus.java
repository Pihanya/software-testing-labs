/*
package ru.itmo.anokhin.testing.lab1.task1S.function;

import java.math.BigDecimal;

public class Cosinus implements AbstractMathFunction {

  private static final BigDecimal DEFAULT_EPSILON = BigDecimal.valueOf(1, -10);

  private BigDecimal epsilon;

  public Cosinus() {
    this(DEFAULT_EPSILON);
  }

  public Cosinus(BigDecimal epsilon) {
    this.epsilon = epsilon;
  }

  @Override
  public DerivableMathFunction derive() {
    for(long i = 0; i < Long.MAX_VALUE; ++i) {

    }

    return null;
  }

  @Override
  public BigDecimal evaluate(BigDecimal x) {

    return null;
  }
}
*/
